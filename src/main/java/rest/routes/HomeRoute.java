package rest.routes;

import rest.HTTPMessage;

// Why not working???
public class HomeRoute extends Route {

    public HomeRoute(HTTPMessage request, HTTPMessage response) {
        super(request, response);
    }
}
